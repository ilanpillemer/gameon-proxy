global
	log /dev/log	local0
	log /dev/log	local1 notice
	chroot /var/lib/haproxy
	stats socket /run/haproxy/admin.sock mode 660 level admin
	stats timeout 30s
	user haproxy
	group haproxy
	daemon

	ssl-default-bind-ciphers kEECDH+aRSA+AES:kRSA+AES:+AES256:RC4-SHA:!kEDH:!LOW:!EXP:!MD5:!aNULL:!eNULL

userlist admins
  user admin insecure-password PLACEHOLDER_PASSWORD

defaults
	log	global
	mode	http
	option	dontlognull
    timeout connect 5000
    timeout client  500000
    timeout server  500000
	errorfile 400 /etc/haproxy/errors/400.http
	errorfile 403 /etc/haproxy/errors/403.http
	errorfile 408 /etc/haproxy/errors/408.http
	errorfile 500 /etc/haproxy/errors/500.http
	errorfile 502 /etc/haproxy/errors/502.http
	errorfile 503 /etc/haproxy/errors/503.http
	errorfile 504 /etc/haproxy/errors/504.http

frontend frontend-ssl
  bind *:443 ssl crt /etc/ssl/proxy.pem
  mode http
  option httplog
  
  acl webapp path_beg -i /app
  use_backend webapp	if webapp
  
  acl player path_beg -i /play
  use_backend player	if player
  
  acl room	 path_beg -i /rooms
  use_backend room		if room
  
  acl concierge path_beg -i /concierge
  use_backend concierge if concierge
  
  acl dockerui path_beg -i /docker
  use_backend dockerui	if dockerui
  
  default_backend website

frontend frontend-plain
  bind *:80
  mode http
  option httplog
  
  acl webapp path_beg -i /app
  use_backend webapp	if webapp
  
  acl player path_beg -i /play
  use_backend player	if player
  
  acl room	 path_beg -i /rooms
  use_backend room		if room
  
  acl concierge path_beg -i /concierge
  use_backend concierge if concierge
  
  acl dockerui path_beg -i /docker
  use_backend dockerui	if dockerui
  
  default_backend website

backend room
  mode http
  option httplog
  balance roundrobin
  server room1 108.168.183.163:9080
  
backend concierge
  mode http
  option httplog
  balance roundrobin
  server concierge1 108.168.183.163:9081

backend player
  mode http
  option httplog
  balance roundrobin
  server player1 108.168.183.163:9443 ssl verify none

backend webapp
  mode http
  option httplog
  balance roundrobin
  server webapp1 108.168.183.163:3000

backend dockerui
  mode http
  option httplog
  balance roundrobin
  server dockerui1 108.168.183.163:9000
  acl auth_ok http_auth(admins)
  http-request auth unless auth_ok
  reqrep ^([^\ :]*)\ /docker(.*)     \1\ /\2  if dockerui
	
backend website
    mode http
    option httplog
    balance roundrobin
    server web1 108.168.183.163:8080
    
listen stats *:1936
    stats enable
    stats uri /
    stats hide-version
    stats auth admin:PLACEHOLDER_PASSWORD
